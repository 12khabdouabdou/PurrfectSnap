<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Optional for in-app APK install flows; ensure policy compliance if on Play -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Clean up legacy storage permissions -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:node="remove" tools:ignore="all" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" tools:ignore="all" />

    <queries>
        <package android:name="com.snapchat.android" />
    </queries>

    <application
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        tools:targetApi="34"
        android:allowBackup="true"
        android:hasFragileUserData="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/launcher_icon">

        <!-- Xposed integration -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="SnapEnhance by rhunk" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedscope"
            android:value="com.snapchat.android" />

        <service
            android:name=".bridge.BridgeService"
            android:exported="true"
            android:permission="com.snapchat.android.permission.UPDATE_STICKER_INDEX" />

        <activity
            android:name=".ui.manager.MainActivity"
            android:theme="@style/AppTheme"
            android:launchMode="singleTask"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.setup.SetupActivity"
            android:launchMode="singleTask"
            android:exported="true"
            android:theme="@style/AppTheme"
            android:excludeFromRecents="true" />

        <activity
            android:name=".bridge.ForceStartActivity"
            android:theme="@android:style/Theme.NoDisplay"
            android:excludeFromRecents="true"
            android:exported="true" />

        <activity
            android:name=".bridge.BiometricPromptActivity"
            android:theme="@style/BiometricPromptTheme"
            android:excludeFromRecents="true"
            android:exported="true" />

        <receiver android:name=".StreaksReminder" />

        <!-- FileProvider for serving the downloaded APK via content:// URI.
             Authority must match ${applicationId}.provider used by the install code. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>
</manifest>
